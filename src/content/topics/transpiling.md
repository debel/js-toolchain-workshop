---
title: "Transpiling to JS"
type: 'lesson'
order: 5
description: "Who wants to write plain old boring JavaScript? Transpilation allow us to write our code in other languages or even implement our custom syntax!"
---

### Babel

- transpile custom syntax to JS
- why it's not used by default (slow, modern compilers already handle common use cases e.g. JSX)
- only needed for very custom syntax

### Esbuild

- What is esbuild?
- Why doesn't vite use esbuild for prod?
- Basic use of esbuild
